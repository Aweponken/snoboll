using UnityEngine;
using System.Collections;

public class solrör : MonoBehaviour {
	// Update is called once per frame
	void Update () {

		transform.Translate(Vector3.left*10* Time.deltaTime);
	}
}
